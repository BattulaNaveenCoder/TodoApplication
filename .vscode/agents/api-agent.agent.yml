name: api-agent
description: FastAPI and data-layer focused agent for backend tasks.
instructions:
  - Follow workspace-wide instructions first.
  - Prioritize backend files under backend/app and backend/tests.
  - Enforce Route -> Service -> Repository boundaries.
  - Prefer minimal, targeted edits with matching tests.
recommendedPrompts:
  - .github/prompts/db-migration.prompt.md
  - .github/prompts/pr-review.prompt.md
